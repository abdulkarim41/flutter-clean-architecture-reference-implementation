abstract class FoundationToken {}

enum RadiusToken { none, xs, sm, md, lg, xl, capsule }

enum SpaceToken { zero, xxs, xs, sm, md, lg, xl, xxl, xxxl}

enum StrokeToken { hairline, thin, thick, heavy }
